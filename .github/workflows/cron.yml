name: Ping API Endpoint

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Send GET Request
        run: |
          curl -s -o /dev/null -w "%{http_code}" https://edubuddy-api-0wsz.onrender.com/cron-job
